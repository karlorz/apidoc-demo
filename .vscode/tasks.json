{
  "version": "2.0.0",
  "tasks": [
    {
        "label": "Docker Compose Up with Wait",
        "type": "shell",
        "command": "docker compose down -v && docker compose up --wait",
        "problemMatcher": [],
        "group": {
            "kind": "build",
            "isDefault": true
        }
    }
]
}